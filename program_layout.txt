//
// Pseudocode
//
Main(){
    GameObjects[];

    // Update should be fixed to ~30fps
    Update(GameObjects[]);

}

Update(GameObjects[]){
    // Possible: Run object scripts in background tasks
    // https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Using_web_workers
    for GameObjects[], if GameObjects has script {
        GameObjects.script();
    }
    DrawGameObjects(GameObjects[]);
    Update(GameObjects[]);
}

DrawgameObjects(GameObjects[]){
    
}